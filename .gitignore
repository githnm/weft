# secrets & env
.env
.env.*
!.env.example

# dependencies & build
node_modules/
dist/
*.tsbuildinfo

# REAL DATA — never publish (customer emails, business data)
prod-models/
posthog-substrate/
funnel-models/
funnel-models_v2/
funnel-models_v3/
semantic-models/
# introspected substrate output (machine-local, regenerated by `introspect`)
substrate/
substrate-*/
**/inspection.json
**/metadata.json
**/review.md
**/*.jsonl

# os & editor
.DS_Store
.vscode/
.idea/
*.log

# local agent/editor tooling (machine-local settings + run configs)
.claude/

# generated artifacts (derived from trace/customer data — never publish)
context-graph.html
*-graph.html
*.context-graph.html

# local connection credentials (machine-only, never commit)
.weft/
connections.json
